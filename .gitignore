# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*~

# Claude Code dev config (machine-specific, not part of the website)
.claude/
.agents/
skills-lock.json

# Local env file (publishable key is public-safe, but keep env files out of the repo)
.env
# ...but DO commit the production env (publishable key only, safe to expose)
!.env.production

# Backup archive (redundant with git history) — remove this line to track it
lumora_media_website.zip
